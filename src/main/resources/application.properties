spring.config.import=optional:file:.env[.properties]
spring.application.name=HaiTeBooks_Backend

# Database configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DATABASE_URL:jdbc:mysql://root:VkzwFNBKuidBkbDEMuDHHhreemezJSqF@hopper.proxy.rlwy.net:35076/haitebooks_db}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:VkzwFNBKuidBkbDEMuDHHhreemezJSqF}

# JPA & Hibernate config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
logging.level.org.springframework.security=INFO

# Server port configuration
server.address=0.0.0.0
server.port=${PORT:8080}

# JWT config
app.jwt.secret=${JWT_SECRET_KEY}
# Access token expiration: 2 giờ (7200000 ms) - cân bằng giữa bảo mật và UX
# Có thể giảm xuống 1 giờ (3600000 ms) nếu cần bảo mật cao hơn
app.jwt.expiration-ms=3600000

# AI configuration
cohere.api.key=${COHERE_API_KEY}

# Environment config
spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}
frontend.url=${FRONTEND_URL:http://localhost:3000}

# VNPAY configuration
vnpay.tmnCode=${VNPAY_TMN_CODE}
vnpay.hashSecret=${VNPAY_HASH_SECRET}
vnpay.payUrl=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.returnUrl=${VNPAY_RETURN_URL:http://localhost:8080/api/v1/payment/vnpay/return}
vnpay.notifyUrl=${VNPAY_NOTIFY_URL:http://localhost:8080/api/v1/payment/vnpay/ipn}